// 이곳에 모든 스타일을 담는다.
// 리셋 스타일을 가져오면 사용자의 기본 스타일과 충돌하여 예상치 못한 스타일 오류를 일으킬 수 있으므로 모든 요소의 스타일을 직접 리셋한다.

#ugly-container *
    font-family -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"
    padding 0
    margin 0

#ugly-container
  width 100%
  height calc(100% - 40px)
  font-size 1.0em
  padding 20px 0

  .table-header
    width 100%
    height 60px

    .header-title
      height 60px
      line-height 60px

      .title-month
        display block
        float left
        width auto
        height 60px
        padding 0 10px
        line-height 60px
        font-size 1.2em
      
      .title-year
        display block
        float left
        width auto
        height 60px
        padding 0 10px
        line-height 60px
        font-weight normal
        font-size 0.6em
  
  .table-body
    table-layout fixed
    border-collapse collapse
    border-spacing 0
    border none
    width 100%
    text-align center
    vertical-align middle
    overflow hidden
    font-size 0.9em

    tr
      border none

    th, td
      position relative
      z-index 1

    th:nth-child(1), th:nth-child(7), td:nth-child(1), td:nth-child(7)
      color #ff4500
    
    .disable
      color #222
      opacity 0.1
    
    .today:after
      content ''
      display block
      position absolute
      z-index -1
      top calc(50% - 10px)
      left calc(50% + 10px)
      width 3px
      height 3px
      border-radius 50%
      background red

    .start-date, .end-date, .selected-dates
      color #fff !important

    .start-date:after, .end-date:after
      content ''
      display block
      position absolute
      z-index -1
      top 0
      left 0
      width inherit
      height inherit
      border-radius 50%
      background #222

    .selected-dates
      border-color red

    .selected-dates:after
      content ''
      display block
      position absolute
      z-index -1
      top 0
      left 0
      width inherit
      height inherit
      background #222
    
    .start-date:after
      border-top-right-radius 0
      border-bottom-right-radius 0

    .end-date:after
      border-top-left-radius 0
      border-bottom-left-radius 0
      